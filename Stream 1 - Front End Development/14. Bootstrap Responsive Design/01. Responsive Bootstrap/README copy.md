# Repsonsive Bootstrap

There was no challenge in this section. We simply have to use the following code in a page to make Bootstrap responsive:

```<meta name="viewport" content="width=device-width, initial-scale=1">```